import styled from 'styled-components';

export const Wrap = styled.div`
  display: flex;
  align-items: center;
  flex-direction: column;
  justify-content: center;
  padding-top: 100px;
`;
{
  /* <thead>
            <tr>
              <th>Name</th>
              <th>Pantone Value</th>
              <th>Year</th>
              <th>Color</th>
            </tr>
          </thead>
          <tbody>
            {usersList?.map((item) => {
              return (
                <tr key={item?.id}>
                  <Wrap.Td color={item?.color}>{item?.name}</Wrap.Td>
                  <Wrap.Td color={item?.color}>{item?.pantone_value}</Wrap.Td>
                  <Wrap.Td color={item?.color}>{item?.year}</Wrap.Td>
                  <Wrap.Td color={item?.color}>{item?.color}</Wrap.Td>
                </tr>
              );
            })}
          </tbody> */
}
